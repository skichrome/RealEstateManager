<?xml version="1.0" encoding="utf-8"?>

<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <group android:checkableBehavior="none">
        <item
            android:id="@+id/action_detailsRealtyFragment_to_addRealtyFragment"
            android:icon="@drawable/ic_edit_24dp"
            android:title="@string/menu_item_edit_name"
            app:showAsAction="ifRoom" />
        <item
            android:id="@+id/searchFragment"
            android:icon="@drawable/ic_search_24dp"
            android:title="@string/menu_item_search_name"
            app:showAsAction="ifRoom" />
    </group>

    <group android:checkableBehavior="none">
        <item
            android:id="@+id/changeCurrentAgent"
            android:title="@string/current_user_actions_change"
            app:showAsAction="never" />
    </group>
</menu>